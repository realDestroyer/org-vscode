# docs/images

Screenshots referenced from documentation.

| File | Used by | What it shows |
|---|---|---|
| `external-api-trust-prompt.png` | [external-api.md](../external-api.md#quickstart) | Modal trust prompt shown the first time a new extension calls `registerLinkType` or `captureTodo`. |
| `external-api-capture-success.png` | [external-api.md](../external-api.md#quickstart) | Confirmation toast after a successful capture from the example consumer. |
| `external-api-disabled.png` | [external-api.md](../external-api.md#quickstart) | Warning shown when `Org-vscode.enableExternalCapture` is `false` and a capture is attempted. |

Drop PNGs into this folder using the names above to make the docs render.
